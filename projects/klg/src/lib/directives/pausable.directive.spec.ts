import { PausableDirective } from './pausable.directive';

describe('PausableDirective', () => {
  it('should create an instance', () => {
    const directive = new PausableDirective();
    expect(directive).toBeTruthy();
  });
});
